<%= form_with model: @user, local: true do |f| %>
  <p><%= f.label :name, '氏名' %><%= f.text_field :name %></p>
  <p><%= f.label :age, "年齢" %><%= f.text_field :age %></p>
  <p><%= f.label :email, "メールアドレス" %><%= f.text_field :email %></p>
  <p><%= f.submit "送信" %></p>
<% end %>